# Apache configuration for Telegram Bot on Render.com
# This file adds to the default Apache configuration

# Add DocumentRoot configuration to the existing Apache config
# The php:apache image already has MPM and other essentials
ServerName localhost
ServerTokens Prod
ServerSignature Off
